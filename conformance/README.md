# Conformance fixtures

This directory will contain deterministic inputs, expected state transitions,
clock assumptions, and error outcomes shared by every implementation.

Fixtures must not depend on wall-clock execution, network timing, Supabase, or
a particular media player.
