# Protocol specifications

This directory contains versioned JSON Schemas and normative protocol
documents. Schemas are the language-neutral source of truth; generated client
types are not.

Version 1 starts with the service manifest, client command and server message
envelopes, canonical room state, and conformance-fixture format. Every schema
uses JSON Schema 2020-12 and a stable `urn:watch-together:` identifier.
